{
  "devDependencies": {
    "esbuild": "^0.14.38"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle src/app.js --outfile=public/app.js --target=es6",
    "start": "npm run build -- --servedir=public",
    "watch": "npm run build -- --watch",
    "server": "node server.mjs"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "socket.io": "^4.5.1",
    "socket.io-client": "^4.5.1"
  }
}
